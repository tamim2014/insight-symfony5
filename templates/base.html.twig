<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>
		<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
		<link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}"/>
		<link
		rel="stylesheet" href="{{ asset('css/top.css') }}"/>
		{# Alignement des colonnes(de chaque tables) #}
		<link rel="stylesheet" href="{{ asset('css/edit_remove.css') }}"/>

		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}

		{% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}
	</head>

	<body>
		<style>
			li.dropdown:hover div.dropdown-menu {
				display: block;
			}
			/* media query symfony: K.O */
		</style>

		<!-- navbar de chez bootswatch  -->
		<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
			<div class="container-fluid">
				<a class="navbar-brand active " href="{{path('accueil')}}">&#127968;</a>
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarColor01">
					<ul class="navbar-nav me-auto">
						<li class="nav-item"></li>
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Products</a>
							<div class="dropdown-menu">
								<a class="dropdown-item" href="#">
									<b>Accès en lecture</b>
								</a>
								<a class="dropdown-item" href="{{path('show_products')}}">&emsp;Products List
								</a>
								<a class="dropdown-item" href="{{path('show_categorie')}}">&emsp;Categories list
								</a>
								<a class="dropdown-item" href="#">
									<b>Accès en écriture</b>
								</a>
								<a class="dropdown-item" href="{{path('new_product')}}">&emsp;Add product</a>
								<a class="dropdown-item" href="{{path('nouvelle_categorie')}}">&emsp;Add category</a>
								{# <a class="dropdown-item" href="/p/edit/{i}">&emsp;Edit product</a> #}
								{# <a class="dropdown-item" href="/c/edit/{i}">&emsp;Edit category</a> #}
								<div class="dropdown-divider"></div>
								{# <a class="dropdown-item" href="/p/remove/{i}">&emsp;Remove product</a> #}
								<a class="dropdown-item" href="{{path('show_edit_remove_product')}}">&emsp;Edit/Remove product</a>
								<a class="dropdown-item" href="#">&emsp;Edit/Remove category</a>
							</div>
						</li>
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Tasks</a>
							<div class="dropdown-menu">
								<a class="dropdown-item" href="#">
									<b>Accès en lecture</b>
								</a>
								<a class="dropdown-item" href="{{path('show_all_task')}}">&emsp;Tasks list
								</a>
								<a class="dropdown-item" href="#">
									<b>Accès en écriture</b>
								</a>
								<a class="dropdown-item" href="{{path('add_task')}}">&emsp;Add task</a>
								{# <a class="dropdown-item" href="#">&emsp;Edit task</a> #}
								<div class="dropdown-divider"></div>
								{# <a class="dropdown-item" href="#">&emsp;Remove task</a> #}
								<a class="dropdown-item" href="{{path('show_edit_remove_task')}}">&emsp;Edit/Remove task</a>
								<a class="dropdown-item" href="{{path('addRemoveTask')}}">&emsp;Add/Remove*<small>
										<i>(en local)</i>
									</small>
								</a>
							</div>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{path('workflow')}}">Work flow</a>
						</li>
					</ul>
					<!-- obligé de mettre le mtr de rechrch à l'exterieur(ici il flotte à gch)  -->
				</div>
				<form class="d-flex">
					<input class="form-control me-sm-2" type="search" placeholder="Search">
					<button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
				</form>
			</div>
		</nav>

		<div class="container">
			{% block body %}
				{# Ce qui est placé ici n'est pas herité #}
			{% endblock %}
		</div>
		{# Ce qui est placé ici est herité #}
		<button onclick="topFunction()" id="myBtn" title="Go to top">
			<img src="images/top.jpg" id="imageToTop">
		</button>
		<script type="text/javascript" src="js/top.js"></script>
	</body>
</html>
